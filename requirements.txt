# ── Existing ML packages ──────────────────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.3.0
imbalanced-learn>=0.11.0
shap>=0.44.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# ── New: MCP server + AI agent ────────────────────────────────────────────────
mcp[cli]>=1.0.0      # MCP protocol — server (FastMCP) and client (SSE)
ollama>=0.3.0        # Ollama Python client for local LLM tool calling
